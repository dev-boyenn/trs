# TRS

Minimal Rust desktop app that opens a window with a Twitch player.

## Prerequisites

- Rust (stable) installed via [rustup](https://rustup.rs/)

## Run

```sh
cargo run -- <twitch_channel>
```

Example:

```sh
cargo run -- ninja
```
